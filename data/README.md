# Data

The files under `synthetic/` are generated data used to demonstrate the project workflow. They do not contain patient records and should not be treated as clinically representative data.

The repository intentionally does not include earlier sample CSV files whose provenance was not documented clearly enough. Keeping only the reproducible synthetic-data path makes the project easier to audit and reproduce.
